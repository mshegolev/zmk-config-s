🔧 Подготовка

Убедись, что установлены:

pip3 install --user west
brew install arm-none-eabi-gcc


Репозитории:

~/zmk — ядро ZMK (клонированное с GitHub).

~/zmk-config-s — твой конфиг (с sofle.keymap, sofle.conf, build.yaml).

Проверь, что в flash_sofle.sh правильные пути:

ZMK_DIR="/opt/develop/zmk"
CONF_DIR="/opt/develop/zmk-config-s/config"
BUILD_YAML="/opt/develop/zmk-config-s/build.yaml"

⚙️ Сборка и прошивка
1. Сборка и прошивка обеих половин
./flash_sofle.sh all


Скрипт соберёт три прошивки:
sofle_left.uf2, sofle_right.uf2, settings_reset.uf2.

Дождись запроса → дважды нажми reset на левой половине.

После записи отключи её, подключи правую, снова дважды reset.

Скрипт прошьёт правую.

2. Прошить только одну половину

Левая:

./flash_sofle.sh left


Правая:

./flash_sofle.sh right

3. Reset-прошивка (сброс BT и настроек)
./flash_sofle.sh reset


⚠️ После этого обязательно снова прошить left и right, иначе клавиатура не будет работать.

🛠 Полезные опции

Пропустить сборку (прошить уже собранное):

./flash_sofle.sh all --no-build


Полностью очистить build/ перед сборкой:

./flash_sofle.sh all --clean


Включить подробные логи сборки:

./flash_sofle.sh all --verbose


Можно комбинировать:

